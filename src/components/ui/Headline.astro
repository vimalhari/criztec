---
import type { Headline as Props } from '~/types';
import { twMerge } from 'tailwind-merge';

const {
  title = await Astro.slots.render('title'),
  subtitle = await Astro.slots.render('subtitle'),
  tagline,
  classes = {},
} = Astro.props;

const {
  container: containerClass = 'max-w-3xl',
  title: titleClass = 'text-3xl',
  subtitle: subtitleClass = 'text-lg',
} = classes;
---

{
  (title || subtitle || tagline) && (
    <div class={twMerge('mb-6 md:mx-auto md:mb-8 text-center', containerClass)}>
      {tagline && (
        <p
          class="text-sm text-primary dark:text-primary font-semibold tracking-wider uppercase mb-2"
          set:html={tagline}
        />
      )}
      {title && (
        <h2
          class={twMerge('font-bold leading-tight tracking-tight font-heading text-heading', titleClass)}
          set:html={title}
        />
      )}

      {subtitle && <p class={twMerge('mt-3 text-muted', subtitleClass)} set:html={subtitle} />}
    </div>
  )
}
